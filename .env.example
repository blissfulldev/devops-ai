# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on vercel

# Get your xAI API Key here for chat and image models: https://console.x.ai/
XAI_API_KEY=****

# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Google Cloud Storage (alternative to Vercel Blob)
# Option A: Single JSON string in GCP_SERVICE_ACCOUNT_KEY
#   - Put the entire service account JSON here (escaped newlines are fine)
# Option B: Split fields (useful on Vercel/GitHub Actions env vars)
GCP_SERVICE_ACCOUNT_KEY=
GCP_PROJECT_ID=
GCP_CLIENT_EMAIL=
GCP_PRIVATE_KEY=
# Required bucket name
GCS_BUCKET=
# Optional public base URL if bucket objects are public or behind CDN
# e.g. https://storage.googleapis.com/<bucket> or https://cdn.example.com
GCS_PUBLIC_BASE_URL=

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/storage/vercel-postgres/quickstart
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****
